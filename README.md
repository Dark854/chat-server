# Chat Server

Node.js Socket.IO server for Android chat app.

## Deploy to Render

1. Create account on render.com
2. New → Web Service
3. Connect GitHub repo OR use Manual Deploy
4. Settings:
   - Build: `npm install`
   - Start: `node server.js`
   - Root: `server` (if using Git)

